bind-key M-\\ run-shell { tmux new-window -a -c "#{pane_current_path}" "~/src/browse-tui/browse-tui ~/src/browse-tui/recipes/browse-claude --pid $(ps -eo pid,ppid,cmd | perl -naE 'say @F[0] if @F[1] == #{pane_pid} && /claude/')" }
